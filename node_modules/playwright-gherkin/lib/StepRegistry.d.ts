import { PlaywrightTestArgs, PlaywrightTestOptions, PlaywrightWorkerArgs, PlaywrightWorkerOptions, TestInfo } from '@playwright/test';
import { DataTable } from './DataTable.js';
import { dialects } from '@cucumber/gherkin';
import { ParsedStep, Step } from './parse.js';
import { ExtractTemplateValues, Template } from './utils.js';
export type PlaywrightArgs = PlaywrightTestArgs & PlaywrightTestOptions & PlaywrightWorkerArgs & PlaywrightWorkerOptions;
type DataTableOrUndefined<Table extends string[][] | undefined> = Table extends string[][] ? DataTable<Table> : undefined;
export type GherkinArgs<Declaration extends StepsDeclaration[number] = StepsDeclaration[number], MatchingTemplate extends string[] = string[], World = Record<string, any>> = {
    docString: Declaration['docString'];
    table: DataTableOrUndefined<Declaration["table"]>;
    step: Declaration;
    world: World;
    parameters: ExtractTemplateValues<MatchingTemplate, Declaration['tokens']>;
};
export type PlaywrightTestInfo = TestInfo;
export type StepFunction<Declaration extends StepsDeclaration[number] = StepsDeclaration[number], Step extends string = string, World = Record<string, any>> = (args: PlaywrightArgs & GherkinArgs<Declaration, Template<Step>, World>, info: PlaywrightTestInfo) => Promise<void>;
export type StepsDeclaration = Array<Pick<Step, 'tokens' | 'text' | 'docString' | 'table'>>;
type ValidStepOrNever<Step extends string, Steps extends StepsDeclaration> = Extract<Steps[number]['tokens'], Template<Step>> extends never ? never : Step;
type ValidStepDeclarations<Step extends string, Steps extends StepsDeclaration> = StepsDeclaration extends Steps ? Steps[number] : Extract<Steps[number], {
    tokens: Template<Step>;
}>;
export declare class StepRegistry<Steps extends StepsDeclaration = StepsDeclaration, World = Record<string, any>> {
    defaultWorld?: World | undefined;
    private steps;
    private dialect;
    constructor(dialect?: keyof typeof dialects, defaultWorld?: World | undefined);
    find<Step extends Steps[number]["text"]>({ tokens, text }: ParsedStep<Step>): {
        call: StepFunction;
        tokens: typeof tokens;
        parameters: string[];
    };
    define<Step extends string>(statement: Steps[number]["text"] | ValidStepOrNever<Step, Steps>, step: StepFunction<ValidStepDeclarations<Step, Steps>, Step, World>): Map<string[], StepFunction<Pick<import("./parse.js").Step, "tokens" | "text" | "docString" | "table">, string, Record<string, any>>>;
}
export {};
//# sourceMappingURL=StepRegistry.d.ts.map